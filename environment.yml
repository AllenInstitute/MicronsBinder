name: micronsbinder
channels:
  - defaults
  - conda-forge
dependencies:
  - matplotlib
  - numpy
  - pandas
  - python=3.7.0
  - pip
  - pip:
    - nglui==2.0.0
    - scipy
    - seaborn
    - cloud-volume
prefix: /opt/anaconda3/envs/micronsbinder

